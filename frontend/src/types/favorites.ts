export interface FavoriteTrain {
  trainNumber: string;
  trainName: string;
  origin: string;
  destination: string;
  addedAt: string;
}

export interface RecentSearch {
  trainNumber: string;
  trainName: string;
  searchedAt: string;
}
